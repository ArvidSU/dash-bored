declare module "*.md" {
  const contents: string;
  export default contents;
}

/** The skill's extensionless agent-tool launcher, embedded as text. */
declare module "*/scripts/dash-bored" {
  const contents: string;
  export default contents;
}
