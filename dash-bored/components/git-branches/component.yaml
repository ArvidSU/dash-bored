schemaVersion: 2
id: git-branches
name: Git branches
description: Lists local Git branches and the commits of work on each branch.
entry: ./index.tsx
propsSchema:
  type: object
  additionalProperties: false
  properties:
    cwd:
      type: string
    baseBranch:
      type: string
  required: []
permissions:
  - process:execute
